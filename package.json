{
  "name": "worktrack-syn",
  "version": "1.0.0",
  "description": "WorkTrack — UPS Engineering Time Tracking Prototype",
  "scripts": {
    "start": "serve . -p ${PORT:-3000} --no-clipboard"
  },
  "dependencies": {
    "serve": "^14.2.4"
  }
}
